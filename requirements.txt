# Python packages needed to run the analysis.
# Install them all with:   pip install -r requirements.txt
#
# The pinned lower bounds are the versions the results were produced with;
# newer versions are expected to work.

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14
matplotlib>=3.7
seaborn>=0.13
tabulate>=0.9          # used by check_convergence.py to write the report table

# Optional — only needed if you want to run notebooks/analysis_main.ipynb
jupyterlab>=4.0
